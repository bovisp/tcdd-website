export const setErrors = (state, payload) => state.errors = payload

export const clearErrors = state => state.errors = {}

export const SET_CONTENT_ID = (state, contentIds) => state.contentIds = contentIds