export const questions = state => state.questions

export const question = state => state.question

export const availableEditors = state => state.availableEditors

export const tempId = state => state.tempId

export const contentIds = state => state.contentIds

export const questionTypeData = state => state.questionTypeData

export const testQuestionData = state => state.testQuestionData